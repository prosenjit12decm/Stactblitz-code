import React from 'react';
import Navbar from './Navbar';
import {useParams} from 'react-router-dom';
export default function Product(){
  return(
    <div>
      {/* <Navbar/> */}
      This is Product section {id}
    </div>
  );
}